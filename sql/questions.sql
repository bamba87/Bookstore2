# connection
# hostname: 127.0.0.1
# username: root
# password: mysql
# database: Bookstore22

#Get all stuff(CHANGE NAME LATER!)
SELECT * FROM atable;

# keyUpSearch
SELECT * FROM books WHERE isbn LIKE "{isbnLike}%";